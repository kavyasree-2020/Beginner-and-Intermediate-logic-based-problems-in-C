#include<stdio.h>
int main() {
  int n,r,c=0,t;
  printf("Enter an integer : ");
  scanf("%d",&n);
  t=n;
  while(n>0)
  {
    r=n%10;
    c++;
    n=n/10;
  }
  printf("Total number of digits of the given number %d = %d\n",t,c);
}